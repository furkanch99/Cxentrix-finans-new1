# Vite 8 ile @vitejs/plugin-react 4.x arasında peer dep conflict var.
# `--legacy-peer-deps` olmadan npm install ERESOLVE hatası veriyor;
# bu dosya sayesinde hem local hem Vercel build'inde otomatik geçerli.
legacy-peer-deps=true
